## Getting Started

First, install the dependencies:

```bash
yarn
```

Second, run the development server:

```bash
yarn dev
```

After starting the server, you will see a login form, to bypass it, enter any username and any password.

To test a production build the project run the command:
```bash
yarn build
# then
yarn start
```
